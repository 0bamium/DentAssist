@model IEnumerable<DentAssist.Models.Data.Entities.Paciente>

@{
    ViewData["Title"] = "Pacientes";
}

<section class="content-header">
    <div class="container-fluid px-0">
        <div class="row mb-2 px-3">
            <div class="col-sm-6">
                <h1>@ViewData["Title"]</h1>
            </div>
            <div class="col-sm-6 text-right">
                <a asp-action="Create" class="btn btn-primary rounded-pill">
                    <i class="fas fa-plus"></i> Nuevo Paciente
                </a>
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="container-fluid px-0">
        <div class="row no-gutters">
            <div class="col-12">
                <div class="card shadow rounded-3 border-0">
                    <div class="card-body px-3">
                        <div class="table-responsive">
                            <table class="table table-bordered table-hover rounded-3 overflow-hidden">
                                <thead class="thead-light">
                                    <tr>
                                        <th>@Html.DisplayNameFor(m => m.First().Nombre)</th>
                                        <th>@Html.DisplayNameFor(m => m.First().Rut)</th>
                                        <th>@Html.DisplayNameFor(m => m.First().Telefono)</th>
                                        <th>@Html.DisplayNameFor(m => m.First().Email)</th>
                                        <th>@Html.DisplayNameFor(m => m.First().Direccion)</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var p in Model)
                                    {
                                        <tr>
                                            <td>@p.Nombre</td>
                                            <td>@p.Rut</td>
                                            <td>@p.Telefono</td>
                                            <td>@p.Email</td>
                                            <td>@p.Direccion</td>
                                            <td>
                                                <a asp-action="Edit" asp-route-id="@p.Id" class="btn btn-sm btn-warning rounded-circle" title="Editar">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <a asp-action="Details" asp-route-id="@p.Id" class="btn btn-sm btn-info rounded-circle" title="Detalle">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@p.Id" class="btn btn-sm btn-danger rounded-circle" title="Eliminar">
                                                    <i class="fas fa-trash"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@model IEnumerable<DentAssist.Models.Data.Entities.Odontologo>

@{
    ViewData["Title"] = "Odontólogos";
}

<section class="content-header">
    <div class="container-fluid px-0">
        <div class="row mb-2 px-3">
            <div class="col-sm-6">
                <h1>@ViewData["Title"]</h1>
            </div>
            <div class="col-sm-6 text-right">
                <a asp-action="Create" class="btn btn-primary rounded-pill">
                    <i class="fas fa-plus"></i> Nuevo Odontólogo
                </a>
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="container-fluid px-0">
        <div class="row no-gutters">
            <div class="col-12">
                <div class="card shadow rounded-3 border-0">
                    <div class="card-body px-3">
                        <div class="table-responsive">
                            <table class="table table-bordered table-hover rounded-3 overflow-hidden">
                                <thead class="thead-light">
                                    <tr>
                                        <th>@Html.DisplayNameFor(m => m.First().Nombre)</th>
                                        <th>@Html.DisplayNameFor(m => m.First().Matricula)</th>
                                        <th>@Html.DisplayNameFor(m => m.First().Especialidad)</th>
                                        <th>@Html.DisplayNameFor(m => m.First().Email)</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var o in Model)
                                    {
                                        <tr>
                                            <td>@o.Nombre</td>
                                            <td>@o.Matricula</td>
                                            <td>@o.Especialidad</td>
                                            <td>@o.Email</td>
                                            <td>
                                                <a asp-action="Edit" asp-route-id="@o.Id" class="btn btn-sm btn-warning rounded-circle" title="Editar">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <a asp-action="Details" asp-route-id="@o.Id" class="btn btn-sm btn-info rounded-circle" title="Detalle">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@o.Id" class="btn btn-sm btn-danger rounded-circle" title="Eliminar">
                                                    <i class="fas fa-trash"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

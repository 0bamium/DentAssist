@model IEnumerable<DentAssist.Models.Data.Entities.Odontologo>
@{
    ViewData["Title"] = "Odontólogos";
}

<section class="content-header">
    <div class="container">
        <div class="row mb-2">
            <div class="col-sm-6"><h1>@ViewData["Title"]</h1></div>
            <div class="col-sm-6 text-right">
                <a asp-action="Create" class="btn btn-primary"><i class="fas fa-plus"></i> Nuevo Odontólogo</a>
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="card card-solid">
                    <div class="card-body">
                        <table class="table table-bordered table-hover">
                            <thead class="thead-light">
                                <tr>
                                    <th>@Html.DisplayNameFor(m => m.Nombre)</th>
                                    <th>@Html.DisplayNameFor(m => m.Matricula)</th>
                                    <th>@Html.DisplayNameFor(m => m.Especialidad)</th>
                                    <th>@Html.DisplayNameFor(m => m.Email)</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var o in Model)
                                {
                                    <tr>
                                        <td>@o.Nombre</td>
                                        <td>@o.Matricula</td>
                                        <td>@o.Especialidad</td>
                                        <td>@o.Email</td>
                                        <td>
                                            <a asp-action="Edit" asp-route-id="@o.Id" class="btn btn-sm btn-warning"><i class="fas fa-edit"></i></a>
                                            <a asp-action="Details" asp-route-id="@o.Id" class="btn btn-sm btn-info"><i class="fas fa-eye"></i></a>
                                            <a asp-action="Delete" asp-route-id="@o.Id" class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
